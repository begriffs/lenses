var L    = require('../lenses'),
  chai   = require('chai'),
  expect = chai.expect;

describe('Lenses', function() {
  'use strict';
  it('exists', function() {
    expect(L).to.be.an('object');
  });
});
